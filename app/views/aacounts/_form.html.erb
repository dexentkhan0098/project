<%= form_with(model: aacount) do |form| %>
  <% if aacount.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(aacount.errors.count, "error") %> prohibited this aacount from being saved:</h2>

      <ul>
        <% aacount.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

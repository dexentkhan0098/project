<%= form_with(model: asuplier) do |form| %>
  <% if asuplier.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(asuplier.errors.count, "error") %> prohibited this asuplier from being saved:</h2>

      <ul>
        <% asuplier.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :age, style: "display: block" %>
    <%= form.number_field :age %>
  </div>

  <div>
    <%= form.label :asuplier_id, style: "display: block" %>
    <%= form.number_field :asuplier_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
